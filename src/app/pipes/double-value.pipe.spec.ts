import { DoubleValuePipe } from './double-value.pipe';

describe('DoubleValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleValuePipe();
    expect(pipe).toBeTruthy();
  });
});
